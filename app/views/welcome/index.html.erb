<%= render "nav.html.erb" %>


<div>
  <% @products.each do |product| %>
     <div>
	     <%= link_to Product.find(product).title, category_product_path(product.category_id, product) %>
	     <div><%= product.text %></div>
	     <div><%= product.price %> сом</div>
     </div>
  <% end %>
</div>