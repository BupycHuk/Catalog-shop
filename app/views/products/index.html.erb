<h1>Все товары</h1>
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>

  <% @products.each do |product| %>
      <tr>
        <td><%= product.title %></td>
        <td><%= link_to 'Show', product_path(product) %></td>
      </tr>
  <% end %>
</table>